# matplot
This program calls the argument from the command line. It opens a file from the command line.It also has a animate function which plots the random values on a graph in a continuous pattern.When the program is executed it displays a graph.
